<%= form_for(choice) do |f| %>
  <% if choice.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(choice.errors.count, "error") %> prohibited this choice from being saved:</h2>

      <ul>
      <% choice.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :text %>
    <%= f.text_area :text %>
  </div>

  <div class="field">
    <%= f.label :question_id %>
    <%= f.text_field :question_id %>
  </div>

  <div class="field">
    <%= f.label :ordinality %>
    <%= f.number_field :ordinality %>
  </div>

  <div class="field">
    <%= f.label :voice_count %>
    <%= f.number_field :voice_count %>
  </div>

  <div class="field">
    <%= f.label :is_pass %>
    <%= f.check_box :is_pass %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
